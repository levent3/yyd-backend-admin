# ================================================
# YYD BACKEND - PRODUCTION ENVIRONMENT
# ================================================
# Bu dosya TEMPLATE'dir. Gerçek şifreler içermez.
# Sunucuda: cp .env.production.example .env.production
# Sonra .env.production dosyasını düzenle!
# ================================================

# ================================================
# APPLICATION
# ================================================
NODE_ENV=production
PORT=5000

# ================================================
# DATABASE - PostgreSQL
# ================================================
# SUNUCUDA DEĞİŞTİR: Güçlü bir şifre kullan!
# Minimum 16 karakter, büyük/küçük harf, rakam, özel karakter
POSTGRES_USER=yyd_prod_user
POSTGRES_PASSWORD=CHANGE_THIS_TO_STRONG_PASSWORD
POSTGRES_DB=yyd_production

# Database Connection URL
# NOT: Şifreyi yukarıdaki ile aynı yap!
DATABASE_URL="postgresql://yyd_prod_user:CHANGE_THIS_TO_STRONG_PASSWORD@postgres:5432/yyd_production?schema=public&connection_limit=20&pool_timeout=20&connect_timeout=10"

# ================================================
# JWT - JSON Web Token
# ================================================
# SUNUCUDA DEĞİŞTİR: Random 32+ karakter oluştur
# Komut: openssl rand -base64 32
JWT_SECRET="CHANGE_THIS_TO_RANDOM_32_CHARS_OR_MORE"
JWT_EXPIRES_IN=7d

# ================================================
# REDIS - Cache
# ================================================
REDIS_URL="redis://redis:6379"

# ================================================
# URLs - Server IP Bazlı
# ================================================
# SUNUCUDA DEĞİŞTİR: 10.200.3.110 yerine sunucu IP'ni yaz
BASE_URL=http://10.200.3.110:5000
FRONTEND_URL=http://10.200.3.110:3000
ADMIN_PANEL_URL=http://10.200.3.110:3000
PUBLIC_WEBSITE_URL=http://10.200.3.110:3000

# ================================================
# CORS - Allowed Origins
# ================================================
# SUNUCUDA DEĞİŞTİR: IP adresini güncelle
CORS_ORIGINS=http://10.200.3.110:3000,http://10.200.3.110:3001,http://localhost:3000

# ================================================
# EMAIL - SMTP (Opsiyonel)
# ================================================
# İleride kullanmak için hazır
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@domain.com
SMTP_PASS=YOUR_EMAIL_PASSWORD

EMAIL_FROM="Yeryüzü Doktorları <bagis@yourdomain.com>"

# ================================================
# PAYMENT GATEWAY - İyzico (Opsiyonel)
# ================================================
# Test ortamı (sandbox)
IYZICO_API_KEY=sandbox-test-key
IYZICO_SECRET_KEY=sandbox-test-secret
IYZICO_BASE_URL=https://sandbox-api.iyzipay.com

# Canlı ortam için:
# IYZICO_API_KEY=your-live-api-key
# IYZICO_SECRET_KEY=your-live-secret-key
# IYZICO_BASE_URL=https://api.iyzipay.com

# ================================================
# RABBITMQ - Message Queue (Opsiyonel)
# ================================================
# Henüz kurulmadı, ileride eklenebilir
# RABBITMQ_URL=amqp://admin:password@rabbitmq:5672/yyd

# ================================================
# LOGGING
# ================================================
LOG_LEVEL=info

# ================================================
# SECURITY
# ================================================
RATE_LIMIT_WINDOW=15
RATE_LIMIT_MAX=100
BCRYPT_ROUNDS=12

# Admin Default Password (Seed için)
# SUNUCUDA DEĞİŞTİR: Güçlü bir şifre belirle!
# Bu şifre ilk admin kullanıcısı için kullanılacak
ADMIN_DEFAULT_PASSWORD=CHANGE_THIS_STRONG_ADMIN_PASSWORD

# ================================================
# NOTLAR
# ================================================
# 1. .env.production dosyasını asla GitHub'a pushlama!
# 2. Güçlü şifreler kullan (min 16 karakter)
# 3. JWT_SECRET için: openssl rand -base64 32
# 4. IP adresini sunucunun gerçek IP'si ile değiştir
# 5. Domain aldığında URL'leri güncelle

# ========================================
# YYD Backend - Environment Configuration
# ========================================

# -----------------
# Server Configuration
# -----------------
NODE_ENV=development
PORT=5000

# -----------------
# Database Configuration
# -----------------
# IMPORTANT: Change these values for production!
# Docker için: postgres:5432
# Local için: localhost:5432 veya localhost:5433 (docker host portuna göre)

# PostgreSQL Connection Variables (Docker konteyneri için)
POSTGRES_USER=yyd_user
POSTGRES_PASSWORD=CHANGE_THIS_SECURE_PASSWORD_IN_PRODUCTION
POSTGRES_DB=yyd_db

# Database URL - Prisma için
# Development (Docker içinden): postgres:5432
# Development (Host'tan): localhost:5433
# Production: Your server's database host
DATABASE_URL="postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}?schema=public&connection_limit=20&pool_timeout=20&connect_timeout=10"

# -----------------
# Redis Configuration
# -----------------
# Development (Docker içinden): redis:6379
# Development (Host'tan): localhost:6379
# Production: Your server's redis host
REDIS_URL="redis://redis:6379"

# -----------------
# JWT Configuration
# -----------------
# IMPORTANT: Generate a strong secret for production!
# You can use: openssl rand -base64 32
JWT_SECRET=CHANGE_THIS_TO_STRONG_SECRET_IN_PRODUCTION_USE_OPENSSL_RAND_BASE64_32
JWT_EXPIRES_IN=7d

# -----------------
# API Configuration
# -----------------
# Base URL for API
BASE_URL=http://localhost:5000

# CORS Origins (comma-separated for multiple origins)
ADMIN_PANEL_URL=http://localhost:3000
PUBLIC_WEBSITE_URL=http://localhost:3001

# -----------------
# File Upload Configuration
# -----------------
# Max file size in bytes (10MB = 10485760)
MAX_FILE_SIZE=10485760
# Allowed file types (comma-separated)
ALLOWED_FILE_TYPES=image/jpeg,image/png,image/gif,image/webp,application/pdf,video/mp4

# -----------------
# Email Configuration (Optional)
# -----------------
# Uncomment and configure for email functionality
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password
# SMTP_FROM=noreply@yyd.org.tr
# SMTP_FROM_NAME=YYD

# -----------------
# Payment Gateway Configuration (Optional)
# -----------------
# Uncomment and configure for payment functionality
# Iyzico
# IYZICO_API_KEY=your_iyzico_api_key
# IYZICO_SECRET_KEY=your_iyzico_secret_key
# IYZICO_BASE_URL=https://sandbox-api.iyzipay.com

# -----------------
# Logging Configuration
# -----------------
# Levels: error, warn, info, http, verbose, debug, silly
LOG_LEVEL=info

# -----------------
# Security Configuration
# -----------------
# Rate limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# -----------------
# Production Checklist
# -----------------
# Before deploying to production, make sure to:
# [ ] Change NODE_ENV to 'production'
# [ ] Update POSTGRES_PASSWORD with a strong password
# [ ] Generate a strong JWT_SECRET
# [ ] Update DATABASE_URL with production database host
# [ ] Update REDIS_URL with production redis host
# [ ] Update BASE_URL with production domain
# [ ] Update ADMIN_PANEL_URL and PUBLIC_WEBSITE_URL
# [ ] Configure SSL certificates
# [ ] Set up proper logging and monitoring
# [ ] Configure backups
